<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs')%>

    <body>
        <% if(users.length> 0) {%>
            <% users.forEach( (user)=> {%>
                <li>
                    <%= user.email %>
                </li>
                <% })} %>
    </body>

</html>